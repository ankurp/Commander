en:  
  activerecord:
    models: 
      command: "Command"
    attributes:
      command:
        text: "Command"
    errors:
      models:
        command:
          attributes:
            text:
              blank: "cannot be blank"